# ZloVPO Server

**Учебный проект по дисциплине "Защита информации от вредоносного программного обеспечения"**

Сервер с JWT аутентификацией и ролевой авторизацией.

---
## Технологии и требования

- Среда разработки **IntelliJ IDEA**
- **Java**
- **Spring Boot** 3.5.5+
- **Maven**
- **HTTP-запросы**: Postman
- **База данных**: PostgreSQL

---
##  **Функциональность**
### Задание 1
- Аутентификация по JWT (access и refresh токены).
- Регистрация новых пользователей.
- Авторизация на основе ролей (USER, ADMIN).
- Хранение refresh токенов в БД (сущность UserSession).
- Защита REST эндпоинтов с проверкой токена и прав доступа.
- Поддержка HTTPS.
- Подключение к PostgreSQL через Spring Data JPA.

### Задание 2
- Реализация структуру таблиц и связей в PostgreSQL по ER-диаграмме
- Реализация операции создания лицензии 
- Реализация операции активации лицензии
- Реализация операции продления лицензии
- Реализация операции проверки лицензии
- Реализация класса `Ticket` для информирования пользователя 

### Задание 3
- Создать хранилище с приватным ключом и публичным сертификатом для создания и проверки подписи.
- Добавить значение публичного ключа в `Github variables`.
- Реализовать компоненты модуля ЭЦП, согласно требованиям.
- Подключить модуль ЭЦП к лицензии для подписания ответов. (Добавление `TicketResponse`).
- Убедиться, что подпись тикета формируется корректно.

### Задание 4
- Реализовать структуру таблиц и связей в PostgreSQL согласно требованиям.
- Реализовать все требуемые операции для работы с сигнатурами.
- Подключить модуль ЭЦП для формирования подписи сигнатур.
- Протестируйте работу всех операций и убедитесь, что:
    - реализованы все 8 операций;
    - create/update действительно пересчитывают подпись;
    - delete является логическим;
    - update и delete пишут запись в History;
    - create/update/delete пишут запись в Audit;
    - полная выгрузка не содержит DELETED;
    - инкремент содержит все записи с updatedAt > since, включая DELETED;
    - историю и аудит можно получить по signatureId.

## Основные эндпоинты

- Аутентификация 
  - `POST /api/auth/login` – вход (access + refresh токены)
  - `POST /api/auth/refresh` – обновление access токена
  - `POST /api/register` – регистрация
- Подсистемы лицензирования
  - `POST /license/create` – создание лицензии (администратор)
  - `POST /license/activate` – активация лицензии (пользователь)
  - `POST /license/renew` – продление лицензии (пользователь)
  - `POST /license/check` – проверка лицензии (пользователь)
- Управление антивирусными сигнатурами
  - `GET /getAll` - Получить все актуальные сигнатуры
  - `GET /getIncrement/{since}` — Получить инкремент изменений, произошедших после указанного момента времени (включая логически удалённые)
  - `GET /getId` — Получить сигнатуры по списку идентификаторов (UUID передаются в теле запроса)
  - `POST /create` — Создать новую сигнатуру (только для администратора)
  - `POST /delete/{id}` — Логически удалить сигнатуру (только для администратора)
  - `PATCH /update/{id}` — Обновить существующую сигнатуру (только для администратора)
  - `GET /getHistory/{id}` — Получить историю изменений сигнатуры (только для администратора)
  - `GET /getAudit/{id}` — Получить аудит действий над сигнатурой (только для администратора)